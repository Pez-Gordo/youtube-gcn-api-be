To run this project you will need a web server with php and mysql modules. Then copy your files into html docs folder. You have to cr
eate the database and run the script "youtube(22).sql" to create the database structure.
